base_url: "https://www.ebay.com"

browsers:
  - name: chromium
  - name: firefox

timeouts:
  default: 10000
  navigation: 30000

retry:
  max_attempts: 3
  backoff_factor: 1.5

search:
  default_limit: 5

credentials:
  use_guest: true